version: '3'
services:
    default-php:
        container_name: default-php
        build:
            context: ./
            dockerfile: default-php/Dockerfile
        volumes:
            - ./:/var/www
        tmpfs:
            - /dev/shm
        restart: unless-stopped
